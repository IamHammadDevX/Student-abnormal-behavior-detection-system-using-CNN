version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./Models:/app/Models:ro
      - ./templates:/app/templates:ro
    environment:
      - TF_CPP_MIN_LOG_LEVEL=2
    restart: unless-stopped
